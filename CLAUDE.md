# 일본어 LLM 위키 — 스키마 & 운영 규칙

이 볼트는 **LLM 위키** 패턴으로 운영하는 일본어 학습 지식베이스다.
관리자(유준모)는 소스를 모으고 질문하며, **위키의 모든 페이지는 LLM이 쓰고 유지한다.**
관리자 프로필: 프로그래머 / JLPT N3 / 일상 회화 가능 / 한자 약함.

목표: 일상에서 발견한 새로운 일본어 **단어·관용어**를 구조화·상호연결해 시간이 지날수록
풍부해지는 개인 위키로 축적한다. 한자에 약하므로 **요미가나와 한국 한자 대응**을 특히 신경 쓴다.

---

## 디렉터리 구조

```
raw/                    # 원본 수집 (불변 소스, 날짜별 파일)
wiki/
  voca/                 # 단어 페이지 (개당 1파일, 예: 袖.md)
  expression/           # 관용어 페이지 (개당 1파일, 예: 油を売る.md)
MOC/
  주제별/               # 주제별 Map of Content
  품사별/               # 품사별 Map of Content
train/                  # 복습·테스트 산출물 (review.md=오전 복습, test.md=저녁 테스트 퀴즈)
templates/              # 페이지 템플릿
.claude/skills/         # LLM 운영 스킬 (ingest, review)
index.md                # 위키 전체 카탈로그 (콘텐츠 지향)
log.md                  # 작업 이력 (시간순, append-only)
CLAUDE.md               # 이 파일 (스키마)
```

**3계층 원칙**
1. **raw/** = 소스 오브 트루스. LLM은 읽기만 하고 수정하지 않는다.
2. **wiki/ · MOC/ · train/** = LLM이 소유·관리하는 산출물. 단, wiki 페이지 프론트매터의 `last_seen`·`복습횟수` 두 필드는 예외로 **사용자가** Meta Bind 복습완료 버튼으로 갱신한다. train/은 review 스킬이 생성한다.
3. **CLAUDE.md** = 규칙. 관리자와 LLM이 함께 발전시킨다.

---

## raw/ 규칙

- 파일명은 날짜: `YYYY-MM-DD.md` (예: `2026-07-26.md`).
- 그날 발견한 단어·관용어를 `## vocabulary` / `## expression` 두 섹션으로 나눠 적는다 (관리자가 직접 적음).
- 프론트매터:
  ```yaml
  ---
  생성일자: 2026-07-26
  ingested: false
  ---
  ```
- 본문 형식:
  ```markdown
  ## vocabulary
  - 袖（そで） : 소매
      - シャツの袖をまくる。      ← 관리자가 직접 찾은 예문 (있을 수도, 없을 수도)
      - source : 요루시카        ← 출처 (있을 수도, 없을 수도)

  ## expression
  - 油を売る : 일하다 말고 딴짓/수다로 시간 허비하는 것
  ```
- **표기 컨벤션**: 단어/관용어 뒤에 `:` 를 붙여 관리자의 메모(뜻 등)를 적는다. 들여쓴 하위 `-` 항목으로:
  - 그 외의 항목 = 관리자가 직접 찾은 **예문** 또는 **출처**.
  - `source :` **또는** `출처 :` 로 시작 = **출처** (예: 요루시카, 책, 대화 등). 인제스트 시 위키 페이지 프론트매터 `tags`에 포함된다.
- **관리자가 직접 넣은 예문은 인제스트 시 위키 페이지 예문에 반드시 포함한다** (아래 인제스트 규칙 참조).
- 인제스트 시: `## vocabulary` 항목은 `wiki/voca/`로, `## expression` 항목은 `wiki/expression/`로 보낸다.
- 인제스트가 끝나면 LLM이 `ingested: true`로 바꾼다.
- **강제 장치 (PreToolUse 훅).** raw 불변 원칙은 문서 약속에 그치지 않고 훅으로 강제된다. `.claude/settings.json`에 등록된 `PreToolUse` 훅이 `.claude/hooks/protect-raw.py`를 실행해, `raw/` 아래 파일을 수정하는 도구 호출(Edit·Write·MultiEdit·NotebookEdit)을 **차단**한다. **유일한 예외**는 인제스트 마지막 단계의 `ingested: false → true` 한 줄 Edit(플래그 플립)뿐이며, 그 외 본문 편집·새 raw 파일 생성은 모두 거부된다. 관리자는 훅과 무관하게 직접 raw를 작성·수정할 수 있다(훅은 도구 호출에만 걸린다).

---

## wiki/voca/ — 단어 페이지 규칙

파일명 = 표제 단어 그대로 (예: `袖.md`).

**프론트매터**
```yaml
---
type: voca
품사: 명사          # 명사 / 동사 / い형용사 / な형용사 / 부사 / 조사 ...
jlpt: N3            # N5~N1, 미상이면 "미상"
기록일자: 2026-07-26
last_seen:          # 마지막 복습 일자 (YYYY-MM-DD). 승격 시엔 공백, 복습 때 갱신
복습횟수: 0          # 복습완료 버튼 누를 때마다 +1. 승격 시 0
aliases: [そで]     # 후리가나 (Obsidian에서 요미가나로도 검색·링크 가능)
tags: [의류, 요루시카]  # 주제 태그와 출처 태그. 주제는 필수, 출처는 raw에 있을 때만
---
```

**본문 구성**
1. 제목: `# 袖 (そで)` — 한자 단어면 요미가나 병기
2. **뜻**: 한글 뜻
3. **요미가나**: 한자 단어인 경우
4. **한자**: 한자 단어인 경우, 한국 한자와 대응되면 `袖 — 소매 수 (한국 한자: 소매 수)` 형태로 뜻·음 표기
5. **예문 3개**: 일본어 + 한글 번역
6. **상위개념**: 상위 개념 페이지로 `[[링크]]`. 상위개념은 그 자체가 실재하는 일본어 개념/단어이므로 **반드시 링크**로 건다 — 그래프에서 계층(IS-A) 트리를 형성하고, 백링크로 하위어를 자동 역인덱스한다. **태그로 쓰지 않는다.** 링크 대상은 일본어 개념명으로 통일한다 (예: `[[衣服]]`, `[[手紙]]`). 대상 페이지가 없으면 인제스트 마지막 단계(아래 인제스트 11번)에서 그 상위개념 페이지도 voca 양식으로 생성한다.
7. **주제**: `#태그` 형태. 주제는 페이지化 대상이 아닌 평면 facet(묶음용 분류)이므로 **반드시 태그**로 쓴다 (예: `#의류`). 같은 이름의 `MOC/주제별/<주제>.md` 허브로 묶는다. **링크로 쓰지 않는다.**
8. **관련어**: 동의어/반의어/관련어. **이미 wiki에 존재하는 페이지에만** `[[링크]]`를 건다. 아직 없는 단어는 링크하지 않고 `—`로 둔다 (상위개념과 달리 생성 후보용 빈 링크를 만들지 않는다).

한자에 약한 관리자를 위해 요미가나·한국 한자 대응을 빠짐없이 채운다.

---

## wiki/expression/ — 관용어 페이지 규칙

파일명 = 관용어 그대로 (예: `油を売る.md`).

**프론트매터** — 기록일자 필수, tags는 출처가 있을 때만
```yaml
---
기록일자: 2026-07-26
last_seen:          # 마지막 복습 일자 (YYYY-MM-DD). 승격 시엔 공백, 복습 때 갱신
복습횟수: 0          # 복습완료 버튼 누를 때마다 +1. 승격 시 0
tags: [요루시카]    # 출처 태그. raw에 출처가 있을 때만 기재, 없으면 이 줄 생략
---
```

**본문 구성**
1. 제목 + 후리가나
2. 3~4줄 이내 설명 (의미 + 유래/뉘앙스)
3. **후리가나**: 관용어 전체 읽기
4. **예문 3개**: 일본어 + 한글 번역
5. **포함 단어 풀이**: 관용어 안에 JLPT N3 이상 난이도 단어가 있으면 그 뜻을 추가 설명. 해당 단어가 wiki/voca/에 있으면 `[[링크]]`

---

## MOC/ 규칙

- **주제별/**: `voca` 단어들을 주제로 묶는다 (예: `의류.md`, `감정.md`). 각 단어를 `[[링크]]`로 나열.
- **품사별/**: 품사로 묶는다 (예: `명사.md`, `동사.md`).
- 단어를 새로 만들면 해당 주제·품사 MOC에도 링크를 추가한다. MOC가 없으면 새로 만든다.

---

## 운영 (Operations)

### 1. Ingest (인제스트)
관리자가 `raw/`에 새 날짜 파일을 넣고 "인제스트해줘"라고 하면:
1. 해당 raw 파일을 읽는다.
2. 단어·관용어를 관리자와 짧게 논의 (필요 시).
3. 단어면 `wiki/voca/`에, 관용어면 `wiki/expression/`에 새 페이지를 만든다. **같은 이름의 페이지가 이미 있으면 그 항목은 스킵한다(덮어쓰거나 갱신하지 않음).** 스킵한 항목은 모아서 마지막에 관리자에게 보고한다.
4. wiki에 페이지 생성 시 양식은 단어의 경우엔 templates/voca, 관용어의 경우엔 templates/expression 을 따른다.
5. **관리자가 raw에 직접 넣은 예문(들여쓴 항목)이 있으면, 그 예문은 위키 페이지 예문에 반드시 포함한다.** 관리자 예문을 첫 예문으로 두고 나머지를 LLM이 채워 예문 3개를 맞춘다. 
6. 단어 파일 작성시 : 관련 상위개념·동의어·반의어 페이지의 링크를 갱신한다.
7. 단어 파일 작성시 : 주제별·품사별 MOC에 링크를 추가한다.
8. `index.md`에 새 페이지를 등록한다.
9. `log.md`에 인제스트 항목을 남긴다.
10. raw 파일의 `ingested: true`로 변경한다.
11. **상위개념 페이지 점검·생성.** 이번 인제스트로 승격된 단어들의 `상위개념` 링크를 점검한다. 대상 상위개념 페이지가 **아직 wiki에 없으면, 그 페이지도 기존 단어와 동일한 voca 양식으로 생성**하고 3~9번(양식·링크·주제/MOC·index·log)을 동일하게 적용한다. 단, 새로 만든 상위개념 페이지의 `상위개념`은 링크(생성 후보)로만 두고 **더 파고들지 않는다 (1레벨에서 멈춤)** — 무한 생성 방지.

인제스트 후 관리자에게 **스킵된 항목(이미 존재해서 처리하지 않은 단어·관용어)** 과 **11번에서 자동 생성된 상위개념 페이지**를 반드시 보고한다.
상호연결·일관성 유지가 핵심.

### 2. Query (질의)
관리자의 질문에 답할 때:
1. `index.md`를 먼저 읽어 관련 페이지를 찾는다.
2. 페이지를 읽고 인용과 함께 답한다.
3. **가치 있는 답변(비교표, 정리, 발견한 연결)은 새 위키 페이지로 파일링**해 대화에 흘려보내지 않는다.

### 3. Lint (점검)
관리자가 "린트"를 요청하면 위키 건강 검진:
- 페이지 간 모순
- 낡은/틀린 설명 (요미가나·한자 오류 포함 — 한자 약한 관리자에게 특히 중요)
- 인바운드 링크 없는 고아 페이지
- 언급됐지만 페이지 없는 개념 (생성 후보)
- 누락된 상호 참조 / MOC 미등록 단어
- 조사해볼 만한 새 질문·소스 제안

### 4. Review (복습)
관리자가 `/review`를 발동하면 (스킬: `.claude/skills/review/SKILL.md`) 하루 단위 복습 루틴을 굴린다.
- **오전 루틴**: `train/review.md`(그날 뽑힌 단어 10개)로 복습.
- **저녁 루틴**: `train/test.md`(직전 세대의 복습 10개를 퀴즈로 렌더한 것)로 테스트.
- 발동 시 동작: ① 기존 `train/review.md`의 10단어를 각 단어 wiki 페이지의 뜻·요미가나·한자를 읽어 `train/test.md` 퀴즈로 렌더(덮어쓰기, 최초 실행이면 스킵) → ② `wiki/voca`의 `last_seen`을 읽어 가장 오래되거나 공백(미복습)인 단어 풀에서 10개를 랜덤 추출해 `train/review.md`를 MOC 스타일 `[[링크]]` 목록으로 갱신. (`train.md` 중간 단계는 폐지 — review.md가 바로 test.md 퀴즈로 렌더된다.)
- **테스트 퀴즈 형식**: 세 유형(① 한자→읽기+뜻 · ② 뜻→단어 · ③ 요미가나→한자)을 10문항에 고르게 섞어 배정하고, 정답은 Obsidian 접기 콜아웃 `> [!success]- 정답`으로 숨긴다. 한자 표기가 없는(가나 표제어) 단어에는 ③을 배정하지 않는다. 문제문에는 `[[링크]]`를 넣지 않는다(정답 한자가 노출되므로).
- **읽기 전용 원칙**: 이 스킬은 wiki 파일을 절대 수정하지 않는다. `뜻`·`요미가나`·`last_seen`을 읽기만 하며, 쓰는 파일은 `train/review.md`·`train/test.md` 둘뿐이다. `last_seen` 갱신은 복습 완료 시 별도로 처리한다(아래 복습완료 버튼).

**복습완료 버튼 (`last_seen`·`복습횟수` 수동 갱신).** 모든 voca·expression 페이지는 **가장 하단(맨 끝)** 에 Meta Bind 버튼 블록을 갖는다. 관리자가 페이지를 열어 복습한 뒤 이 버튼을 누르면 **① `last_seen`이 오늘 날짜로 갱신**되고 **② `복습횟수`가 +1** 된다. 버튼 아래 표시줄에 `VIEW[{last_seen}]`·`VIEW[{복습횟수}]`로 현재 값이 보인다. 이 버튼 블록은 `templates/voca.md`·`templates/expression.md`에 포함돼 있으므로 인제스트로 만드는 새 페이지에도 자동으로 들어간다 — **블록을 임의로 제거하지 않는다.** 블록 형식:
```meta-bind-button
label: ✅ 복습완료 (오늘로)
style: primary
actions:
  - type: updateMetadata
    bindTarget: last_seen
    evaluate: true
    value: "moment().format('YYYY-MM-DD')"
  - type: updateMetadata
    bindTarget: 복습횟수
    evaluate: true
    value: "(x ?? 0) + 1"
```
  - **필요 플러그인/설정**: Obsidian에 **Meta Bind** + **js-engine** 플러그인 설치, Meta Bind 설정에서 **JavaScript 활성화**, 날짜 포맷 **`YYYY-MM-DD`**(추출 스킬의 정렬과 일치시키기 위함).

---

## index.md & log.md

- **index.md**: 콘텐츠 카탈로그. 카테고리(단어/관용어/MOC)별로 각 페이지를 `[[링크]] — 한 줄 요약`으로 나열. 인제스트마다 갱신.
- **log.md**: 시간순 append-only. 각 항목은 `## [YYYY-MM-DD] 종류 | 제목` 형식으로 시작해 unix 도구로 파싱 가능하게 유지. 종류 = `ingest` / `query` / `lint`.
  - review 실행은 로그를 남기지 않는다(매일 반복되는 루틴이라 의도적으로 생략).

---

## 규칙 요약 (Do / Don't)

- ✅ 모든 위키 페이지는 LLM이 작성·유지한다.
- ✅ 요미가나·한국 한자 대응을 항상 채운다.
- ✅ **계층 관계(상위개념)=링크, 평면 분류(주제)=태그.** 둘을 혼용하지 않는다.
- ✅ 상위개념 링크는 아낌없이 건다. 대상 페이지가 아직 없어도 `[[링크]]`를 걸어 생성 후보로 남긴다.
- ✅ 프론트매터 `tags`는 주제 태그(필수) + 출처 태그(선택)로 관리한다 (예: `[의류, 요루시카]`).
- ⚠️ 단, **동의어/반의어/관련어는 이미 wiki에 있는 페이지에만** 링크한다. 없는 단어로 빈 링크를 만들지 않는다.
- ✅ 페이지를 만들면 index·MOC·log를 반드시 갱신한다.
- ✅ 복습 상태(`last_seen`·`복습횟수`)는 사용자가 페이지 하단 복습완료 버튼으로만 갱신한다. LLM·스킬은 이 두 필드에 쓰지 않는다.
- ❌ raw/ 파일의 학습 내용을 수정하지 않는다 (ingested 플래그만 변경). **PreToolUse 훅(`.claude/hooks/protect-raw.py`)이 이를 강제** — 플래그 플립 외의 raw/ 수정은 도구 단에서 차단된다.
- ❌ 페이지 하단의 Meta Bind 복습완료 버튼 블록을 제거·변형하지 않는다.
- ❌ 확실하지 않은 요미가나·뜻은 추측하지 말고 미상으로 표기하거나 확인한다.
